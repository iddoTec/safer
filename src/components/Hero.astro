---
import hero from "../../public/img/hero.mp4";
---

<section id="hero" class="scroll-mt-20 mt-10 sm:mt-0 sm:px-4">
    <video class="sm:rounded-3xl pt-2 sm:pt-0" id="video" autoplay muted>
        <source src={hero} type="video/mp4" />
        Your browser does not support the video tag.
    </video>
</section>
